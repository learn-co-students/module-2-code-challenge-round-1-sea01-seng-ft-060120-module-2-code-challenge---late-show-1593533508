<%= form_for @appearance do |f| %>
Guest Name: <%= collection_select(:guest, :guest_id, Guest.all, :id, :name) %><br>
Episode: <%= collection_select(:episode, :episode_id, Episode.all, :id, :date)%><br>
  <%= f.submit %>
<% end %>
